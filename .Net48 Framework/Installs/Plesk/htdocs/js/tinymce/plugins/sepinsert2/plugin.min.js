tinymce.PluginManager.add('sepinsert2', function (editor, url) {

    editor.ui.registry.addMenuButton('sepinsert2', {
        text: 'Insert',
        fetch: function (callback) {
            var items = [
                {
                    type: 'menuitem',
                    text: 'Newest Content',
                    onAction: function () {
                        WYSIWYGFunctions("newestcontent", true, true);
                    }
                }
            ];
            callback(items);
        }
    });

});